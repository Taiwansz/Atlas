{
  "name": "@atlas/engine-payroll",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "engines/payroll",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python3 -m unittest discover -s tests -v",
        "cwd": "engines/payroll"
      }
    }
  }
}
